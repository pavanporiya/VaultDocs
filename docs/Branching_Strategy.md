# Branching Strategy

> This document will define the branching model used by the DocFlow team.

## Contents (To Be Completed)

- Branch naming conventions
- Long-lived branches: `main`, `develop`
- Short-lived branches: `feature/*`, `bugfix/*`, `hotfix/*`, `release/*`
- Branch naming format: `type/TICKET-ID-short-description`
- Branch protection rules for `main` and `develop`
- Feature branch lifecycle (create → develop → PR → review → merge → delete)
- Release branch workflow
- Hotfix process for production emergencies
- Environment-to-branch mapping (develop → staging, main → production)
- Branch cleanup policy
